# Backend
PORT=4000

# Frontend (Vite injects at build time; use for LAN access)
# Leave empty or omit to use dev proxy to localhost:4000
VITE_API_BASE_URL=

# Asset generation script only (not used at runtime)
GEMINI_API_KEY=
